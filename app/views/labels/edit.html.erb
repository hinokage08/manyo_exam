<h2>ラベル編集</h2>
<%= form_with(model: @label, local: true) do |f| %>

  <%= f.label :name %>
  <p><%= f.text_field :name %></p>

  <%= f.submit "ラベル更新" %>
<% end %>

<%= link_to "ラベル一覧", labels_path %>
