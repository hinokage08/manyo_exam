<h1>タスク一覧</h1>

<table>
  <tr>
    <th>タスク名</th>
    <th>内容</th>
  </tr>

<% @tasks.each do |blog| %>
  <tr>
    <td><%= task.title %></td>
    <td><%= task.content %></td>
  </tr>
<% end %>
</table>
